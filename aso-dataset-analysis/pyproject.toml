[project]
name = "aso-dataset-analysis"
version = "0.1.0"
description = "ASO Atlas vs ASOptimizer dataset compatibility analysis"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "marimo>=0.20.4",
    "matplotlib>=3.10.8",
    "pandas>=3.0.1",
    "pydantic>=2.12.5",
]

[project.scripts]
report = "generate_report:main"
